{
  "buildCommand": "cd frontend && npm install && npm run build && echo '=== Build complete ===' && ls -la && echo '=== Checking build dir ===' && ls -la build",
  "outputDirectory": "frontend/build",
  "env": {
    "REACT_APP_API_URL": "https://critcoin-platform-production.up.railway.app"
  }
}
